---
// This file is intentionally empty.
// Blog posts are now served from src/pages/[...slug].astro at the top level.
// The vercel.json redirect handles /blog/:slug → /:slug with a 301.
// No pages should be generated from this file.
export function getStaticPaths() {
  return [];
}
---
